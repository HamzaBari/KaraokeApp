
public interface StackIterable<T> { //This is a generic stack iterable to iterate teh stack elements. 

    //this is an abstarct function for an iterator which is used the iterate the stackElements. 
    public stackIterator<T> iterator();

}
