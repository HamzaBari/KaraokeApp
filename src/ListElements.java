
public interface ListElements<T> extends Iterable<T> {  //This extends a iterable class whoch is generic. This is uses to iterate elements from the list. 

    public boolean contains(Object o);  //This function would check if it contains the elements in the list or not.

}
